import { Stack, Typography } from "@mui/material";

const Profile = () => {
  return (
    <Stack
      spacing={6}
      sx={{ marginTop: "2rem", alignItems: "cneter", justifyContent: "center" }}
    >
      <Typography variant="h2">Username here</Typography>
    </Stack>
  );
};

export default Profile;
